# The origin the app is served from, without a trailing slash.
# Used for canonical links, Open Graph URLs and the generated sitemap.
# Leave unset locally: the app falls back to the browser's own origin, and the
# build skips sitemap.xml (which requires absolute URLs).
VITE_SITE_URL=https://roam.example.com
